from setuptools import find_packages, setup
setup(
    name='hexutils',
    packages=find_packages(include=['hexutils']),
    version='0.1.0',
    description='Hex utility to find a sequence of bytes in a binary file and stuff.',
    author='https://github.com/personnumber3377',
    license='MIT',
    install_requires=[],
    setup_requires=[]
)
